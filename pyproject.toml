[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tricompose"
version = "0.0.1"
description = "Privacy-conscious adapters for dynamic frozen-model composition"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

